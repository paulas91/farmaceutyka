<%= form_with(model: @syrup) do |form| %>
  <% if @syrup.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@syrup.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
        <% @syrup.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3 col-4">
    <%= form.label :name, class: "form-label" %>
    <%= form.select :name, ["Axoprofen Forte (200mg/5ml)", "Ibum Forte (200mg/5ml)", "Ibum (100mg/5ml)"], class: "form-select" %>
    <% syrup.errors.full_messages_for(:syrup).each do |message| %>
      <div><%= message %></div>
    <% end %>

  <div class="mb-3"><br>
    <%= form.label :dose_time, class: "form-label "  %>
    <%= form.datetime_select("dose_time")%>
  </div>


  <div class="mb-3">
    <%= form.submit class: "btn btn-success" %>
  </div>
<% end %>
